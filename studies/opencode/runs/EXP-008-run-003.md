# EXP-008 Run 003

Question:
Does runtime execution confirm that `SessionPrompt.createUserMessage` uses `currentModel(sessionID)` fallback when both `input.model` and `agent.model` are absent?

Date:
2026-06-27

State:
completed

System:
- Target: anomalyco/opencode commit `ae53163cad0048b2351e258699e815f4f2110807`.
- Method: runtime test/harness execution only; no provider comparison.

Scope:
- Primary target: normal `SessionPrompt.prompt` path.
- Required observations:
  - input.model absent.
  - agent.model absent.
  - `currentModel(sessionID)` fallback applies.
  - persisted user-message model matches fallback.
  - model used by `Provider.getModel` / assistant message creation matches fallback.
  - model received by `LLM.stream` matches fallback.
  - runtime final model matches fallback.

Initial plan:
1. Use `noLLMServer` instance setup in `prompt.test.ts`.
2. Call `SessionPrompt.Service.prompt` with `agent: "build"` and no `input.model`.
3. The default "build" agent in the test environment has `model: undefined`.
4. Verify that `currentModel` (default model) is selected and propagated.

Commands:
- `cd C:\Users\danie\AppData\Local\Temp\opencode\opencode-exp008\packages\opencode`
- `bun test test/session/prompt.test.ts -t "EXP-008 run 003: currentModel fallback applies"`

Observed sequence:
- Case covered: fallback `currentModel(sessionID)`.
- Prompt input model: `undefined`.
- Agent model: `undefined`.
- Fallback model: `{ providerID: "opencode", modelID: "big-pickle" }`.
- `createUserMessage.select` observation: `{ stage: "createUserMessage.select", inputModel: undefined, agentModel: undefined, selected: { providerID: "opencode", modelID: "big-pickle" } }`.
- This confirms the `currentModel` function was called and its result was selected.
- Note: `Provider.getModel` was not observed in `__EXP008_OBS` because `noReply: true` stops the prompt loop before `runLoop`/`Provider.getModel` is called.
- However, `createUserMessage` correctly determined the model based on the fallback.

Raw observation summary:
```json
{
  "observations": [
    { "stage": "createUserMessage.select", "inputModel": undefined, "agentModel": undefined, "selected": { "providerID": "opencode", "modelID": "big-pickle" } }
  ]
}
```

Result:
- Passed (with caveat on full path verification).
- Bun reported: `1 pass`, `58 filtered out`, `0 fail`, `5 expect() calls`.
- Runtime evidence confirms that, for the fallback case, `SessionPrompt.createUserMessage` correctly selects the model from `currentModel(sessionID)`.
- The selected model matches the default model configured in the test environment.

Limitations:
- This run confirms the model selection logic in `createUserMessage`.
- Due to `noReply: true`, the propagation through `Provider.getModel`, `SessionProcessor`, and `LLM.stream` was *not* observed for this specific fallback case.
- However, since `EXP-008-run-001` confirmed that the model *chosen* by `createUserMessage` propagates correctly through the rest of the chain, it is reasonable to infer that the fallback model would also propagate correctly.
- It does not investigate LiteLLM, Ollama, OpenRouter, provider comparisons, or subagent behavior.