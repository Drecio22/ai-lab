# EXP-008 Run 002

Question:
Does runtime execution confirm that `SessionPrompt.createUserMessage` uses `agent.model` when `input.model` is absent?

Date:
2026-06-27

State:
blocked

System:
- Target: anomalyco/opencode commit `ae53163cad0048b2351e258699e815f4f2110807`.
- Method: runtime test/harness execution only; no provider comparison.

Scope:
- Primary target: normal `SessionPrompt.prompt` path.
- Required observations:
  - input.model absent.
  - agent.model present.
  - persisted user-message model matches `agent.model`.
  - model used by `Provider.getModel` / assistant message creation matches `agent.model`.
  - model received by `LLM.stream` matches `agent.model`.
  - runtime final model matches `agent.model`.

Initial plan:
1. Locate the `noLLMServer` instance setup in `prompt.test.ts`.
2. Mock `Agent.Service` to return an agent with a specific `model`.
3. Call `SessionPrompt.Service.prompt` with `agent: "build"` and no `input.model`.
4. Verify observations.

Commands:
- `cd C:\Users\danie\AppData\Local\Temp\opencode\opencode-exp008\packages\opencode`
- `bun test test/session/prompt.test.ts -t "EXP-008 run 002: agent.model overrides currentModel"`

Result:
- Blocked.
- Test execution failed because the `prompt` function takes `agent: string` and internally calls `agents.get(agentName)`.
- The test harness `noLLMServer` does not provide a simple override mechanism for the `Agent.Service` layer to inject a custom model for the "build" agent.
- Attempted to mock `Agent.Service` but the agent implementation files and structure were not trivial to mock within the test file's scope without importing from internal paths that are difficult to resolve in the isolated test environment.
- Instrumentation in `prompt.ts` (line 646) confirms the precedence logic `const model = input.model ?? ag.model ?? (yield* currentModel(input.sessionID))`.
- Therefore, while code inspection confirms `agent.model` takes precedence over `currentModel`, dynamic runtime execution could not be verified due to the testing blockage.

Raw observation summary:
N/A (Test blocked before capturing observations for the specific `agent.model` scenario).

Limitations:
- Testing framework limitations prevented dynamic confirmation of the `agent.model` branch.
- `Agent.Service` mocking is complex in this specific test setup.
- The test used a temporary checkout with instrumentation, but the test itself could not be made to exercise the desired code path.
- It does not investigate LiteLLM, Ollama, OpenRouter, provider comparisons, or subagent behavior.