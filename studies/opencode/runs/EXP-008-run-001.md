# EXP-008 Run 001

Question:
Does runtime execution confirm that `SessionPrompt.createUserMessage` stores the model that `SessionPrompt.runLoop`, `Provider.getModel`, and `LLM.stream` later use for the same prompt?

Date:
2026-06-27

State:
completed

System:
- Target: anomalyco/opencode commit `ae53163cad0048b2351e258699e815f4f2110807`.
- Method: runtime test/harness execution only; no provider comparison.

Scope:
- Primary target: normal `SessionPrompt.prompt` path.
- Required observations:
  - input model received by prompt path.
  - agent model, if applicable.
  - `currentModel(sessionID)` fallback, if applicable.
  - persisted user-message model.
  - model used by `Provider.getModel` / assistant message creation.
  - model received by `LLM.stream`.
  - runtime final model.

Initial plan:
1. Search for an existing OpenCode test/harness that exercises `SessionPrompt.createUserMessage -> runLoop -> SessionProcessor.process -> LLM.stream`.
2. Prefer an existing test over new test code.
3. If temporary instrumentation is required, make it in a temporary checkout and revert it after execution.
4. Cover the smallest case first: explicit `input.model`.
5. Cover `agent.model` and fallback `currentModel(sessionID)` only if the existing harness makes that low-cost.

Commands:
- Checkout temporal:
  - `git clone --filter=blob:none --no-checkout https://github.com/anomalyco/opencode.git C:\Users\danie\AppData\Local\Temp\opencode\opencode-exp008`
  - `git checkout ae53163cad0048b2351e258699e815f4f2110807`
- Dependency setup:
  - `$env:PATH = "$env:APPDATA\npm;$env:PATH"; & "$env:APPDATA\npm\bun.cmd" install`
  - Temporary environment workaround: `bun add zod@4.4.3 --dev`, plus temporary junctions/stubs under `node_modules` so `ai` could resolve `zod/v4` in this Windows/Bun checkout.
- Test execution:
  - `$env:PATH = "$env:APPDATA\npm;$env:PATH"; & "$env:APPDATA\npm\bun.cmd" test test/session/prompt.test.ts -t "EXP-008 observes explicit input model through prompt to runtime"`
- Revert instrumentation:
  - `git restore --worktree -- package.json bun.lock packages/opencode/src/provider/provider.ts packages/opencode/src/session/llm.ts packages/opencode/src/session/processor.ts packages/opencode/src/session/prompt.ts packages/opencode/test/session/prompt.test.ts; git status --short`

Temporary instrumentation:
- `packages/opencode/src/session/prompt.ts`: pushed observations for `currentModel`, `createUserMessage.select`, and `runLoop.getModel.result` into `globalThis.__EXP008_OBS`.
- `packages/opencode/src/provider/provider.ts`: pushed `provider.getModel.result` observations.
- `packages/opencode/src/session/processor.ts`: pushed `processor.process.input` observations.
- `packages/opencode/src/session/llm.ts`: pushed `llm.stream.input` and `llm.runtime.ai-sdk` observations.
- `packages/opencode/test/session/prompt.test.ts`: added one temporary test named `EXP-008 observes explicit input model through prompt to runtime`.
- All tracked instrumentation and dependency manifest changes were reverted after the run. `git status --short` in the checkout returned no output.

Observed sequence:
- Case covered: explicit `input.model` only.
- Prompt input model: `{ providerID: "test", modelID: "test-model" }`.
- Agent model: absent for built-in `build` agent in this test; not used.
- `currentModel(sessionID)`: not called in this test because `input.model` was explicit.
- Persisted user message model: `{ providerID: "test", modelID: "test-model" }`.
- `Provider.getModel` result: `{ providerID: "test", modelID: "test-model" }`.
- `SessionPrompt.runLoop` model after `getModel`: `{ providerID: "test", modelID: "test-model" }`.
- `SessionProcessor.process` input model: `{ providerID: "test", modelID: "test-model" }`.
- `LLM.stream` input model: `{ providerID: "test", modelID: "test-model" }`.
- Runtime selected: `ai-sdk`.
- Final runtime request body observed by `TestLLMServer`: `model: "test-model"`.

Raw observation summary:
```json
{
  "observations": [
    { "stage": "createUserMessage.select", "inputModel": { "providerID": "test", "modelID": "test-model" }, "selected": { "providerID": "test", "modelID": "test-model" } },
    { "stage": "provider.getModel.result", "providerID": "test", "modelID": "test-model" },
    { "stage": "runLoop.getModel.result", "providerID": "test", "modelID": "test-model" },
    { "stage": "processor.process.input", "providerID": "test", "modelID": "test-model" },
    { "stage": "llm.stream.input", "providerID": "test", "modelID": "test-model" },
    { "stage": "llm.runtime.ai-sdk", "providerID": "test", "modelID": "test-model" }
  ],
  "inputs": [
    { "model": "test-model" }
  ]
}
```

Result:
- Passed.
- Bun reported: `1 pass`, `57 filtered out`, `0 fail`, `8 expect() calls`.
- Runtime evidence confirms that, for explicit `input.model`, the same model selected by `SessionPrompt.createUserMessage` is persisted on the user message, materialized by `Provider.getModel`, passed to `SessionProcessor.process`, received by `LLM.stream`, and sent to the final AI SDK runtime request.

Limitations:
- This run covers only the explicit `input.model` branch.
- It does not dynamically cover the `agent.model` branch.
- It does not dynamically cover the `currentModel(sessionID)` fallback branch.
- It uses temporary instrumentation in a throwaway checkout, not committed test code.
- It exercises the AI SDK runtime path, not the native runtime path.
- It does not investigate LiteLLM, Ollama, OpenRouter, provider comparisons, or subagent behavior.
